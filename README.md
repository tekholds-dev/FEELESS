# FEELESS

Black-and-green terminal UI for the FEELESS ecosystem. It has no fabricated CA, market data, charts, or liquidity routes.

## Local development

```sh
cp .env.example .env
npm install
npm run dev
```

Set official HTTPS URLs in `.env` to enable Pump, DEX, and Swap. Until then, those controls display **Coming Soon** and are disabled.
